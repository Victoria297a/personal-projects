# 🏗️ ParkPulse Sofia - Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER BROWSER                             │
│                     http://localhost:3000                        │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ HTTP Requests
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND (React + Vite)                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Pages Layer                                               │  │
│  │  • Home.tsx    • Events.tsx    • About.tsx               │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Components Layer                                          │  │
│  │  • MapView     • SearchBar     • AlertsPanel              │  │
│  │  • Navbar      • Layout                                   │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Services Layer                                            │  │
│  │  • api.ts (Axios wrapper for API calls)                  │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Libraries                                                 │  │
│  │  • React Router  • Leaflet  • TailwindCSS  • TypeScript  │  │
│  └───────────────────────────────────────────────────────────┘  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             │ API Calls (/api/*)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BACKEND (Node.js + Express)                    │
│                     http://localhost:5000                        │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Routes Layer                                              │  │
│  │  • /api/parking-zones     • /api/events                  │  │
│  │  • /api/locations         • /api/predictions             │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Business Logic                                            │  │
│  │  • Distance calculation (Haversine formula)              │  │
│  │  • Recommendation scoring algorithm                       │  │
│  │  • Event filtering by date range                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Data Layer (Mock JSON)                                    │  │
│  │  • parking-zones.json    • events.json                   │  │
│  │  • locations.json                                         │  │
│  └───────────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  Middleware                                                │  │
│  │  • CORS (Cross-Origin)   • Express JSON Parser           │  │
│  └───────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

## Data Flow Diagram

```
User Action (Click/Search)
        │
        ▼
React Component State Update
        │
        ▼
API Service Call (Axios)
        │
        ▼
Express Route Handler
        │
        ▼
Read Mock JSON Data
        │
        ▼
Process & Calculate
        │
        ▼
Return JSON Response
        │
        ▼
Update Component State
        │
        ▼
Re-render UI with New Data
```

## Component Hierarchy

```
App
├── Router
    ├── Layout
    │   ├── Navbar
    │   └── Outlet
    │       ├── Home
    │       │   ├── SearchBar
    │       │   ├── MapView (Leaflet)
    │       │   └── AlertsPanel
    │       ├── Events
    │       │   └── EventList
    │       └── About
    │           └── InfoSections
```

## API Endpoints Structure

```
/api
├── /parking-zones
│   ├── GET /              → List all zones
│   └── GET /:id           → Get specific zone
├── /events
│   ├── GET /              → List all events
│   ├── GET /active        → Get upcoming events
│   └── GET /:id           → Get specific event
├── /locations
│   ├── GET /              → List all locations
│   ├── GET /?type=school  → Filter by type
│   └── GET /:id           → Get specific location
└── /predictions
    └── GET /?lat=&lng=    → Get recommendations
```

## File Structure

```
parking/
├── frontend/
│   ├── src/
│   │   ├── components/      # Reusable UI components
│   │   │   ├── Layout.tsx
│   │   │   ├── Navbar.tsx
│   │   │   ├── MapView.tsx
│   │   │   ├── SearchBar.tsx
│   │   │   └── AlertsPanel.tsx
│   │   ├── pages/          # Route pages
│   │   │   ├── Home.tsx
│   │   │   ├── Events.tsx
│   │   │   └── About.tsx
│   │   ├── services/       # API integration
│   │   │   └── api.ts
│   │   ├── types/          # TypeScript definitions
│   │   │   └── index.ts
│   │   ├── App.tsx         # Main app component
│   │   ├── main.tsx        # Entry point
│   │   └── index.css       # Global styles
│   ├── public/             # Static assets
│   ├── index.html          # HTML template
│   ├── package.json
│   ├── vite.config.ts
│   ├── tsconfig.json
│   └── tailwind.config.js
│
├── backend/
│   ├── routes/             # API route handlers
│   │   ├── parking.js
│   │   ├── events.js
│   │   ├── locations.js
│   │   └── predictions.js
│   ├── data/               # Mock JSON data
│   │   ├── parking-zones.json
│   │   ├── events.json
│   │   └── locations.json
│   ├── server.js           # Express server
│   └── package.json
│
├── README.md              # Main documentation
├── QUICK_START.md         # Getting started guide
├── PROJECT_SUMMARY.md     # Complete overview
├── COMMANDS.md            # Command reference
├── start-dev.bat          # Windows startup script
└── start-dev.sh           # Linux/Mac startup script
```

## Technology Stack Layers

```
┌─────────────────────────────────────────┐
│         PRESENTATION LAYER              │
│  React Components + TailwindCSS         │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         APPLICATION LAYER               │
│  React Hooks + State Management         │
│  Leaflet Map Integration                │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         COMMUNICATION LAYER             │
│  Axios HTTP Client + REST API           │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         BUSINESS LOGIC LAYER            │
│  Express Route Handlers                 │
│  Calculation Algorithms                 │
└─────────────────────────────────────────┘
                  │
┌─────────────────────────────────────────┐
│         DATA LAYER                      │
│  Mock JSON Files (parking, events)      │
└─────────────────────────────────────────┘
```

## Map Integration Architecture

```
MapView Component
        │
        ├── Leaflet Core
        │   ├── Base Tile Layer (OpenStreetMap)
        │   ├── Parking Zone Polygons (Colored by availability)
        │   ├── Location Markers (Schools/Hospitals/Offices)
        │   └── Event Markers (Upcoming events)
        │
        └── Interactive Features
            ├── Click Zones → Show Popup
            ├── Zoom Controls
            └── Center View Button
```

## State Management Flow

```
Initial Load
    │
    ├── Fetch Parking Zones    → Update zones state
    ├── Fetch Events           → Update events state
    └── Fetch Locations        → Update locations state
            │
User Interaction
    │
    ├── Search Input           → Filter locations
    ├── Toggle School Mode     → Show/hide schools
    └── Click Zone             → Show zone details
            │
Map Updates
    │
    └── Re-render with new markers/polygons
```

## API Response Format Examples

### Parking Zone
```json
{
  "id": "zone-1",
  "name": "Center",
  "availability": "low",
  "availabilityScore": 15,
  "coordinates": [[42.6977, 23.3219], ...],
  "pricePerHour": 2.5
}
```

### Event
```json
{
  "id": "event-1",
  "title": "Levski vs CSKA Derby",
  "parkingImpact": "high",
  "impactScore": 95,
  "recommendedParking": [...]
}
```

### Location
```json
{
  "id": "school-1",
  "name": "91 German Language School",
  "type": "school",
  "coordinates": [42.6827, 23.3169],
  "safetyZone": true
}
```

## Deployment Architecture

```
GitHub Repository
        │
        ├── Frontend → Vercel
        │   • Automatic builds
        │   • CDN distribution
        │   • Environment variables
        │
        └── Backend → Render
            • Node.js environment
            • Auto-deploy on push
            • Environment variables
```

---

This architecture supports:
- ✅ Scalability
- ✅ Maintainability
- ✅ Clear separation of concerns
- ✅ Easy testing
- ✅ Simple deployment
